# AlphaGPT (A股截面因子挖掘) - Python 依赖
# Python 3.13（与 gm SDK 对齐）
#
# 安装：pip install -r requirements.txt
# 注意：本机清华镜像若不可达，torch 需走官方 CPU 源：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# 其余包走官方 PyPI：--index-url https://pypi.org/simple

# 深度学习与数值计算
torch>=2.0
numpy
pandas
scipy

# 数据源（GMtest utils 层的运行时依赖）
gm                        # 掘金量化 SDK
pyarrow                   # parquet 缓存 I/O

# 工具
tqdm
pytest
