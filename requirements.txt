# AlphaGPT (A股截面因子挖掘) - Python 依赖
# Python 3.13（与 gm SDK 对齐）
#
# 安装：pip install -r requirements.txt
# 默认走官方 PyPI；Windows 下 PyPI 的 torch 即 CPU 版，无需额外索引。

# 深度学习与数值计算
torch>=2.0
numpy
pandas
scipy

# 数据源（GMtest utils 层的运行时依赖）
gm                        # 掘金量化 SDK
pyarrow                   # parquet 缓存 I/O

# 工具
tqdm
pytest
